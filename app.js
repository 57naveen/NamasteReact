const heading = React.createElement("h1",{},"Hello from React") //create the element 

const root = ReactDOM.createRoot(document.getElementById("root")) //create the root

root.render(heading); //render the heading into the root